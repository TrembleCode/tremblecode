# docs/assets

Place demo media here. The README references `docs/assets/demo.gif` — record a
~30–60s screen capture of the core flow (new project → discuss PRD → generate
plan → approve → watch the board/agents → preview) and drop it here as
`demo.gif` before the public launch.
